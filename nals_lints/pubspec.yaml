name: nals_lints
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependency_overrides:
  analyzer: ^6.4.1

dependencies:
  custom_lint_builder: ^0.6.4
  source_gen: ^1.5.0
